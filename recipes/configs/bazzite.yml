modules:
  - type: default-flatpaks
    notify: true
    user:
      install:
        - com.discordapp.Discord
        - org.openrgb.OpenRGB
  - type: rpm-ostree
    install:
      - goverlay
      - mangohud
  - type: systemd
    system:
      disabled:
        - tailscaled.service
