modules:
  - type: files
    files:
      - source: system
        destination: /

  - from-file: common-packages.yml
  - from-file: common-flatpaks.yml

  - type: systemd
    user:
      enabled:
        - sshd.service
